import { DimensionValue } from "react-native";

export const lineHeights = {
  title: "28px",
  copy: "20px",
};

export const space: Record<"sm" | "md" | "base" | "lg" | "xl", DimensionValue> =
  {
    sm: 4,
    md: 8,
    base: 16,
    lg: 32,
    xl: 64,
  };
