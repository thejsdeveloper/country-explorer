import { colors } from "./colors";
import { sizes } from "./size";
import { lineHeights, space } from "./space";
import { fontSizes } from "./fonts";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fontSizes,
};
